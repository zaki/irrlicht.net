#include "main.h"

extern "C"
{
	//EXPORT void WriteFile_Close(IntPtr writefile);

    // TODO:
    // EXPORT int       WriteFile_Write(IntPtr writefile, IntPtr buffer, int sizeToWrite);
	// EXPORT bool      WriteFile_Seek(IntPtr writefile, long finalPos, bool relativeMovement);
	// EXPORT long      WriteFile_GetPos(IntPtr writefile);
	// EXPORT M_STRING  WriteFile_GetFileName(IntPtr writefile);
}
